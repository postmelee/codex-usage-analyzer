# Experimental pet fixtures

This directory contains synthetic metadata only. The tests create bounded PNG
and WebP header fixtures in an operating-system temporary directory at runtime.
No real Codex home path, selected pet identifier, image, profile, or credential
is stored here.
